# Bugs in the 70-480 exam

* Use of `document.removeChild(element);`. This should be something like `el.parentNode.removeChild...` (page 29)
* Unsupported methods removeNode replaceNode (about 33 page)

